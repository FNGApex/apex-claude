// Package version carries the single source of truth for the Apex Claude
// binary version. Both `apex version` and scripts/publish.ps1's version
// resolution read this const.
package version

const Version = "0.2.0"
