# Follow-ups index

Auto-generated by `apex followups render`.

## findings
- [011] risk — signals fingerprint hashes only manifest name+size and top-level dir names: a same-size manifest edit and any change under internal/ read as fresh (verified 2026-09-13: refresh reported fresh while signals.md was months stale)
- [012] nit — docs fingerprint (internal/docs) is size-only too: an edit that keeps a doc's byte length reads as unchanged
- [013] nit — .signalsignore documents exclude/[generated] glob modes but nothing reads it: no Go code, agent, or command references the file
- [014] nit — reminders never leave status open: a due reminder re-nudges every session start forever; only 'apex reminder rm' clears it (no ack/done)
- [015] nit — handoff.Status: gitHead swallows errors, so its 'outside a repo -> 1' branch is dead; a handoff doc outside git reports 2 STALE (HEAD moved) instead
- [016] risk — unverified: install scripts pair set -u with 'for arg in "$@"'; bash < 4.4 (stock macOS 3.2) may abort with unbound variable on the no-arg curl|bash path. macOS CI does not run the installers
- [017] nit — unverified: session-start's detached 'apex update check --quiet' was only observed when the hook ran from a shell/PowerShell, not under Claude Code itself (does the child survive the hook runner on each OS?)
